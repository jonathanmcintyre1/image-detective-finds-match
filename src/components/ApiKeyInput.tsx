// This component is no longer used as we're now using service account credentials
// through an edge function for Google Cloud Vision API authentication.
// Keeping this file as a placeholder to maintain file structure.

import React from 'react';

const ApiKeyInput = () => {
  return null;
};

export default ApiKeyInput;
